local req = (syn and syn.request) or (http and http.request) or http_request or request
local list = game:GetService("HttpService"):JSONDecode(req({Url="https://httpbin.org/get",Method="GET"}).Body)
for i,v in ipairs({"SW-Fingerprint","Syn-Fingerprint","Flux-Fingerprint","Krnl-Hwid"}) do
    if rawget(list.headers,v) then
        method = v
        hwid = rawget(list.headers,v)
    end
end
if not (hwid and method) then
    return setclipboard(("Unknown Exploit: %s"):format(identifyexecutor()))
end
setclipboard(hwid)
hint = Instance.new("Hint",game.CoreGui)
hint.Text = "Successfully copied HWID to clipboard"
delay(5,function() hint:destroy() end)
